To run Flash!

1.) Clone the repo
2.) Downlowd JDOM and include the extracted .jar files in the project's build path
3.) Edit the run configuration to pass in the filename of choice (currently passing in az900.xml) as an argument
